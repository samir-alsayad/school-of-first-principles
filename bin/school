#!/bin/bash
# School CLI Wrapper
# Usage: ./bin/school status

# Ensure we are in the project root
cd "$(dirname "$0")/.."

# Run the school package
export PYTHONPATH=$PYTHONPATH:$(pwd)/src
python3 -m school "$@"
